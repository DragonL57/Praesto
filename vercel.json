{
    "buildCommand": "pip install -r api/requirements.txt && pnpm run build",
    "crons": [
        {
            "path": "/api/cron/cleanup-unverified-users",
            "schedule": "0 3 * * *"
        }
    ]
}